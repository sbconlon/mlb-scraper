# Dumper abstract base class

class Dumper:
    def __init__(self):
        pass

    def dump_state(self, game):
        assert(False) # Should not be called

    def dump_lines(self, game):
        assert(False) # Should not be called
